- defaults:
    shell:
      stdin: true
      stdout: true
      stderr: true
    link:
      create: true
      relink: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing modules]
  - [curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim, Installing vim-plug]
  - [fish -c "curl -L https://get.oh-my.fish > omf; and fish omf --path=~/.local/share/omf --noninteractive; /bin/rm omf; true", Installing oh-my-fish]

- link:
    ~/.dotfiles: ""
    ~/.tmux.conf: tmux/.tmux.conf
    ~/.vimrc: vim/.vimrc
    ~/.config/fish/config.fish: fish/config.fish
